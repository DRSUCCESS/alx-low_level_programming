#include "main.h"

/**
 * _abs - function that computes the absolute value of an integer
 * Return: a or -a
 * @a: parameter of the function
 */

int _abs(int a)
{

	if (a < 0)
		return (-a);
	else
		return (a);


}
